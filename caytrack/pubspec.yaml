name: caytrack
description: "Smart Tracking for Ceylon."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.0

  # --- Firebase Dependencies Updated to fix compilation errors ---
  firebase_core: ^2.27.0 
  cloud_firestore: ^4.16.0
  firebase_auth: ^4.17.0
  # -----------------------------------------------------------------
  
  # --- MOVED PACKAGES: These are used in your application code ---
  url_launcher: ^6.1.11
  font_awesome_flutter: ^10.7.0 
  # -----------------------------------------------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  # url_launcher and font_awesome_flutter removed from here.

flutter:

  uses-material-design: true

  assets:
    - assets/logo/logo.png
    - assets/logo/logo2.png
    - assets/developer/developer_photo.png